## 🧱 مرحله 2 — ساخت entry point برنامه

در Go همیشه نقطه شروع:

`main.go`

ساخت:

`touch main.go`

محتوا:

`package main  import "fmt"  func main() {     fmt.Println("Hello Temporal") }`

اجرا:

`go run .`