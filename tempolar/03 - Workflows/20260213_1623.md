---
created: 2026-02-13
updated: 2026-02-13 16:23
tags: []
---

# {{Temporal-Retry-Policy-Code-Example}} 

## هدف

این قطعه کد نحوه‌ی تعریف و استفاده از سیاست بازتلاش را به صورت کامل نشان می‌دهد:

```

import (
    "time"
    "go.temporal.io/sdk/workflow"
    "go.temporal.io/sdk/temporal" // ۱. Import پکیج ضروری
)

func GreetSomeone(ctx workflow.Context, name string) (string, error) {
    // ۲. تعریف مقادیر سیاست بازتلاش
    retrypolicy := &temporal.RetryPolicy{
        InitialInterval:    15 * time.Second, // اولین تلاش بعد از ۱۵ ثانیه
        BackoffCoefficient: 2.0,              // دو برابر کردن تاخیر در هر بار شکست
        MaximumInterval:    time.Second * 60, // سقف تاخیر: ۶۰ ثانیه
        MaximumAttempts:    100,              // توقف بعد از ۱۰۰ بار شکست
    }

    // ۳. متصل کردن Policy به گزینه‌های اکتیویتی
    options := workflow.ActivityOptions{
        StartToCloseTimeout: time.Second * 5,
        RetryPolicy:         retrypolicy, // اعمال سیاست سفارشی
    }
    
    ctx = workflow.WithActivityOptions(ctx, options)

    var result string
    err := workflow.ExecuteActivity(ctx, app.GreetInSpanish, name).Get(ctx, &result)
    
    return result, err
}

## مراحل
```
1. 
2. 
3. 

## ابزارها
- 

## بهبودهای آینده
- 


لینک ها : 

[[20260213_1620|Temporal-Activity-Retry-Implementation]]

----

## 🔄 Processing Status
- [ ] #to-process   ← خام (هنوز فکر اولیه است)
- [ ] #to-link      ← هنوز به نوت‌های دیگر لینک نشده
- [ ] #to-expand    ← نیاز به تکمیل و توضیح بیشتر
- [x] #to-test      ← باید در عمل تست شود
- [ ] #to-review    ← نیاز به بازبینی و اصلاح
- [ ] #ready        ← آماده استفاده و استناد
