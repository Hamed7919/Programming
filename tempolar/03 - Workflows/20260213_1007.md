---
created: 2026-02-13
updated: 2026-02-13 10:06
tags: []
---

# {{Temporal-Activity-Execution-Steps}} 

## هدف
...

## مراحل

برای اجرای یک اکتیویتی در ورک‌فلو، سه گام اصلی وجود دارد:

1. **تعریف تنظیمات (Activity Options):** تعیین پارامترهایی مثل Timeout.
    
    - `StartToCloseTimeout`: حیاتی‌ترین گزینه که باید همیشه تنظیم شود.
        
2. **ایجاد Context جدید:** استفاده از `workflow.WithActivityOptions` برای تزریق تنظیمات به Contextِ ورک‌فلو.
    
3. **فراخوانی ExecuteActivity:** درخواست از کلاستر برای اجرای تابع اکتیویتی.
    
    - این متد یک **Future** برمی‌گرداند، چون اجرا ناهمگام (Async) است.

## ابزارها
- ### قطعه کد نهایی (Cheat Sheet)

این را می‌توانید در یک نت مجزا یا در انتهای نتِ `03` قرار دهید:

Go

```
```
// ترکیب تنظیمات، اجرا و دریافت نتیجه در یک ساختار تمیز
func GreetSomeone(ctx workflow.Context, name string) (string, error) {
    options := workflow.ActivityOptions{
        StartToCloseTimeout: time.Second * 5, // تشخیص کرش ورکر
    }
    ctx = workflow.WithActivityOptions(ctx, options)

    var spanishGreeting string
    // زنجیره کردن Execute و Get
    err := workflow.ExecuteActivity(ctx, GreetInSpanish, name).Get(ctx, &spanishGreeting)
    if err != nil {
        return "", err // مدیریت خطای احتمالی
    }

    return spanishGreeting, nil
}
```

```
## بهبودهای آینده
- 


لینک ها : 
[[20260213_0944|Temporal-Activity-Definition]]
[[20260213_0954|Temporal-Activity-Registration]]

----
## 🔄 Processing Status
- [ ] #to-process   ← خام (هنوز فکر اولیه است)
- [ ] #to-link      ← هنوز به نوت‌های دیگر لینک نشده
- [ ] #to-expand    ← نیاز به تکمیل و توضیح بیشتر
- [ ] #to-test      ← باید در عمل تست شود
- [ ] #to-review    ← نیاز به بازبینی و اصلاح
- [ ] #ready        ← آماده استفاده و استناد
