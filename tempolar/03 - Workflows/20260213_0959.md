---
created: 2026-02-13
updated: 2026-02-13 09:59
tags: []
---

# {{Temporal-Worker-Init-Code}} 

## هدف
...

## مراحل

یک نمونه استاندارد برای مقداردهی اولیه Worker که هم ورک‌فلو و هم اکتیویتی را شامل می‌شود:

Go

```
```
func main() {
    // ایجاد کلاینت
    c, _ := client.Dial(client.Options{})
    defer c.Close()

    // ایجاد ورکر در یک Task Queue مشخص
    w := worker.New(c, "greeting-tasks", worker.Options{})

    // ثبت ورک‌فلو
    w.RegisterWorkflow(app.GreetSomeone) 

    // ثبت اکتیویتی (تفاوت فقط در نام متد است)
    w.RegisterActivity(app.GreetInSpanish) 

    // شروع به کار ورکر
    w.Run(worker.InterruptCh())
}
```
```
## ابزارها
- 

## بهبودهای آینده
- - **نکته معماری**: یک Worker می‌تواند همزمان مسئولیت اجرای ورک‌فلو و اکتیویتی را بر عهده بگیرد (مانند مثال بالا).
    
- **یادآوری مهم**: باز هم یادتان باشد که بعد از اضافه کردن خط `RegisterActivity` به کد، طبق قاعده [[20260213_0932|Temporal-Worker-Restart-Requirement]]، باید ورکر را **Restart** کنید.


لینک ها : 


----
## 🔄 Processing Status
- [ ] #to-process   ← خام (هنوز فکر اولیه است)
- [ ] #to-link      ← هنوز به نوت‌های دیگر لینک نشده
- [ ] #to-expand    ← نیاز به تکمیل و توضیح بیشتر
- [ ] #to-test      ← باید در عمل تست شود
- [ ] #to-review    ← نیاز به بازبینی و اصلاح
- [ ] #ready        ← آماده استفاده و استناد
