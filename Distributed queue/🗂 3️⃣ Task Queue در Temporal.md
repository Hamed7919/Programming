Task Queue کانالی است که Worker ها از آن کار دریافت می‌کنند.

اگر:

- Workflow روی queue A باشد
    
- Worker روی queue B گوش دهد
    

Workflow اجرا نخواهد شد.

در Dashboard باید بررسی شود:

- نام Task Queue درست باشد
    
- حداقل یک Worker متصل باشد