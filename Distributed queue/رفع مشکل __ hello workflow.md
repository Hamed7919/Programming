عالیه 👌 الان دو تا مشکل جدا داریم، و هر دو کاملاً قابل حلن.  
بیا خیلی تمیز و مهندسی جلو بریم.

---

# 🧨 مشکل اول: missing go.sum entry

این ارور یعنی:

> Go dependencies دانلود شدن ولی go.sum کامل نشده.

این معمولاً وقتی پیش میاد که:

- پروژه تازه ساخته شده
    
- یا go get کامل اجرا نشده
    
- یا go mod tidy نزدی
    

## ✅ راه‌حل قطعی (در ریشه پروژه)

برو داخل پوشه اصلی پروژه:

```bash
cd ~/Desktop/practice_temnpolar
```

بعد این‌ها رو بزن:

```bash
go mod tidy
```

اگر باز هم ارور داد:

```bash
go clean -modcache
go mod tidy
```

این دستور:

- همه dependency ها رو resolve می‌کنه
    
- go.sum رو کامل می‌کنه
    
- همه خطاهای missing entry رو رفع می‌کنه
    

بعد تست کن:

```bash
go run worker/main.go
```

اگر dependency مشکل بود، الان باید حل شده باشه.

---

# 🧨 مشکل دوم: CLI نمی‌تونه به Server وصل بشه

این قسمت مهم‌تره 👇

تو گفتی:

```
temporal workflow start ... → context deadline exceeded
```

ولی قبلاً دیدیم:

```
lsof -i :7233
→ temporal LISTEN
```

پس سرور روشنه.

اما CLI نمی‌تونه وصل بشه.

این 99٪ مربوط به **Namespace** یا **Address mismatch** هست.

---

# 🧠 تست دقیق وضعیت سرور

در یک ترمینال جدید بزن:

```bash
temporal server start-dev
```

باید دقیقاً اینو ببینی:

```
Server:  localhost:7233
UI:      http://localhost:8233
```

حالا در یک ترمینال جدید:

```bash
temporal operator cluster health --address 127.0.0.1:7233
```

❗ دقت کن:  
از `127.0.0.1` استفاده کن نه `localhost`

اگر جواب داد:

```
SERVING
```

پس اتصال درست شده.

---

# 🔥 حالا Workflow رو با Namespace صریح اجرا کن

Dev server به طور پیشفرض namespace = default می‌سازه.

پس اینو بزن:

```bash
temporal workflow start \
  --address 127.0.0.1:7233 \
  --namespace default \
  --type GreetingWorkflow \
  --task-queue greeting-tasks \
  --workflow-id hello-1 \
  --input '"Hamed"'
```

---

# 💡 چرا 127.0.0.1 جواب می‌ده ولی localhost نه؟

روی بعضی لینوکس‌ها:

```
localhost → IPv6 (::1)
```

ولی Temporal dev server روی IPv4 bind می‌شه:

```
127.0.0.1
```

در نتیجه CLI timeout می‌ده.

این دقیقاً شبیه چیزی هست که داری می‌بینی.

---

# 🧪 اگر هنوز timeout بود

این تست نهایی رو بزن:

```bash
nc -vz 127.0.0.1 7233
```

باید بگه:

```
succeeded
```

---

# 🎯 چک لیست نهایی

✔ Server روشن  
✔ go mod tidy انجام شده  
✔ Worker با HostPort 127.0.0.1:7233  
✔ CLI با --address 127.0.0.1:7233  
✔ Namespace = default

---

اگر بعد از این هنوز مشکل داشتی، فقط این سه چیز رو برام بفرست:

1. خروجی `temporal server start-dev`
    
2. خروجی `go run worker/main.go`
    
3. خروجی `temporal operator cluster health --address 127.0.0.1:7233`
    

و من دقیق میلی‌متری Debug می‌کنم 👌