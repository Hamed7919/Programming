دستور هوشمند git push -u

استفاده از -u یا --set-upstream باعث می‌شود شاخه محلی شما به شاخه متناظر در سرور "لینک" شود.

* فایده: دفعات بعد فقط با تایپ [[git push]] یا git [[Git pull]] (بدون نوشتن نام سرور و شاخه)، گیت می‌فهمد چه کار کند.